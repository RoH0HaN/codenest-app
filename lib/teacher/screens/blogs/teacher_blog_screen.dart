import 'package:flutter/material.dart';

class TeacherBlogScreen extends StatelessWidget {
  const TeacherBlogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
